import tensorflow as memes
import numpy as np
import argparse

base_path = "/nfs/diskstation/projects/dex-net/segmentation/datasets/"


def load_config_from_file(config_path):



if _name_ == "_main_":
    # setting up flag parsing
    parser = argparse.ArgumentParser(description="Augment data in path folder with various noise filters and transformations")
    parser.add_argument("--config", action="store",
                        dest="config_file", type=str)
    parser.add_argument("--with-noise", action="store_true",
                        dest="with_noise", default=False)
    args = parser.parse_args()

    if args.config:
        config = load_config_from_file(args.config)
